rpc:
  transport: tcp # 服务间 RPC 统一使用 Origin TCP 长连接。
  tcp:
    send_queue_messages: 16384 # 每条 TCP 连接最多等待发送的完整 RPC 消息数。
    read_idle_timeout: 15s # TCP 读空闲和应用层心跳周期；0s 表示关闭。
    write_timeout: 15s # 写出一个完整 TCP RPC 帧的最长时间。
